# 交接筆記（2026-08-13）

專案的技術與業務規則寫在 `CLAUDE.md`（Claude Code 會自動載入）。
這份只記**當下的資料狀態、未完成事項、以及這段期間踩過的坑**。

線上版本：https://mamibuy.github.io/heheyen-recon/ ・最新 commit `cbdb798`

---

## 1. 資料庫現況（2026-08-13 實測）

`shipping_orders` 共 **115 筆**

| 平台 | 筆數 |
|---|---|
| 官網 | 62 |
| LINE商城 | 27 |
| 蝦皮 | 24 |
| 兆豐 | 2 |

| 狀態 | 筆數 |
|---|---|
| 已入帳 | 111 |
| 已對帳 | 1 |
| 平台已結算 | 3 |

月份分布：2025-12(1)、2026-01(24)、02(11)、03(13)、04(18)、05(20)、06(15)、07(13)

**資料完整度**：`sa_no`、`order_date` 全數有值；2 筆缺 `fee_total`（連 `payable` 也是空的）——
LINE商城 `202602260120000684`(2/26, 應收 2490)、`202602230450003265`(2/23, 應收 2780)。

**尚未收尾的 3 筆「平台已結算」**（有應入帳、無實際入帳）：
兆豐 `607020108`(3,666)、兆豐 `607130099`(3,290)、LINE商城 `202601090430000091`(1,061.89)。

**目前有入帳差異的 4 筆**：

| 平台 | 訂單 | 應入帳 | 實際 | 差異 |
|---|---|---|---|---|
| 官網 | SUQU-MQ2G-L4H1 | 2,416 | 2,410 | −6 |
| 官網 | YSV5-MR4G-2LNG | 2,112 | 2,110 | −2 |
| 官網 | P4WZ-MRHB-74KV | 479 | 473 | −6 |
| 蝦皮 | 260725HPR9J9B5 | 2,985 | 2,977 | −8 |

四筆都還沒填差異原因（`diff_note` 全表皆空）。

---

## 2. 這期間新增的功能（2026-08-11 → 08-13）

| commit | 內容 |
|---|---|
| `25ad4e6` | 編輯訂單可改「手續費」；蝦皮未開立訂單的「代開發票金額」欄改顯示回推的應開金額 |
| `8f1b453` | 發票分組卡片可收合 |
| `cbdb798` | 入帳差異可記錄原因與對應發票（含附件）；蝦皮明細表底部加代開發票扣款說明 |

細節寫在 `CLAUDE.md`，這裡只記三個容易踩到的點：

- **改手續費不會自動重算應入帳。** 兩欄各自獨立，要一起改。蝦皮「應開發票金額」是用
  `payable + fee_total` 回推的，只改一邊會讓該金額跟著位移。
- **未開立的訂單顯示的是「應開」金額**（灰字加註），刻意不讀 `order_invoice_amount` ——
  舊版 parser 曾把代收金額寫進該欄，六月三筆留有殘值，且不會隨應收／手續費更動而同步。
- **`diff_note` 不可改用 `note`。** `note` 是出貨報表帶進來的買家訂單備註
  （DB 裡實際存著「星期一至五收包裹」「618活動⋯」），且 `reconcile` 自動建檔會寫入
  「對帳單匯入」，混用會互相蓋掉。

`sql/07_add_diff_note.sql` **已在正式資料庫執行**（五個 `diff_*` 欄位）。

---

## 3. 未完成／待決事項

### 蝦皮代開發票的邊界情況
現行「應開發票金額」用 `payable + fee_total` 回推，等同 I~O 欄加總。
目前報表 J/K/L/N/O 欄全為 0，故與使用者定義的 `I − M` 一致
（7 月報表已實測：M 欄有 −200 優惠券的那筆也吻合）。
**若日後出現退款、蝦幣回饋券或買家支付運費，兩者會分歧**，屆時要改成從報表逐欄取值
並新增 DB 欄位儲存。

### 六月三筆未開立訂單的 `order_invoice_amount` 殘值
`2606131UP5YJ0M`(2730)、`260618BY897N3R`(2750)、`260620H4F8YA1S`(2780) 曾留有舊版 parser
寫入的值。**這三筆現已歸戶到 `DK13654450`**，實開金額會被覆寫成 14320，殘值問題自然消失。
新版顯示邏輯在未開立時不讀該欄，所以即使日後又出現殘值也不會顯示錯。

### 兆豐福利網只做了對帳，沒做出貨轉換
兆豐訂單目前**只能靠對帳單自動建檔**進來。訂單明細報表其實有完整收件人／地址／電話／商品，
可以做成 `parsers.js` 的第五個 parser 走正常出貨流程餵 ERP。使用者說「先不用，先對帳就好」。
注意：兆豐是**填真實值**那一派（同官網、LINE商城），不是蝦皮酷澎的隱碼。

### 兆豐的「資料來源提示」留空
其他通路的步驟卡都有「預設路徑：⋯」告訴使用者去後台哪裡下載，兆豐留空
（`SOURCE_HINT.recon`），使用者說要自己編輯 SOP 補。

### 附件只在差異視窗有「移除」
`diff_invoice_pdf_url` 可以在視窗裡移除（同時刪 Storage 檔案）。
但整筆訂單被刪除時，Storage 裡的附件不會跟著刪，會變成孤兒檔案。
手續費／交易處理費發票的 PDF 也有同樣情況（既有行為，非這次新增）。

---

## 4. 這段期間修掉的坑（避免重蹈）

| 症狀 | 真因 |
|---|---|
| 天心銷貨單比對回填說「找不到 SA 開頭的單號」 | 表頭在第 7 列，`sheet_to_json` 預設取第 0 列 |
| 蝦皮進帳檔上傳後完全沒反應／代收付金額空白 | 表頭在第 6 列，整份解析出 0 筆 |
| 酷澎訂單出現「應收 0 / 應入帳 -4」 | 上傳成「營業稅明細」而非「出帳明細」，parser 讀不到欄位全變 0，再套上寫死的 4 元 |
| 官網›LINE Pay 應收全是 0 | D-2「交易查詢表」用的是 `收款金額` 不是 `付款金額` |
| 官網›LINE Pay 手續費全是 0 | D-1 檔案裝的是**別的月份**的交易，與 D-2 零交集（非程式問題） |
| LINE商城 LINE Pay 應收停在 0 | parser 算了 `total` 卻沒放進回傳物件 |
| 自動建檔的訂單沒有訂單日期 | parser 沒回傳 `order_date` |
| 清掉入帳日後訂單消失在選取清單 | 清單只看 `recon_status`，沒看 `in_date` |
| 「7 月這兩筆代開發票金額沒帶入」 | **不是上傳的檔案有問題**。該欄存的是「實際開立金額」，未開立本來就空；是六月三筆留有舊版 parser 的殘值，才顯得不一致 |
| 「東西被刪掉了」 | 多半是**瀏覽器快取舊版**。部署後務必提醒 ⌘⇧R |

**通則**：報表解析出問題時，先確認「表頭在第幾列」和「欄位名稱是否與 parser 的 pick 清單相符」，
再去看數字邏輯。上述有一半以上都是這兩件事。
**但也不要預設是報表的問題** —— 上面那筆「沒帶入」查下來是顯示邏輯，檔案完全正確。

---

## 5. 驗證與部署慣例

- **不要只靠 build 成功就宣告完成。** 這段期間至少三次 build 過了但功能是壞的。
- 本地可跑 `npm run dev` 驗證（記得開 `/heheyen-recon/` 路徑，根目錄是空白頁）。
  要進入 App 直接設 `localStorage.setItem('hhy_auth','1')`，**不要輸入密碼**。
- 需要驗證會寫 DB 的按鈕時，用攔截法：覆寫 `window.fetch`，把非 GET 的請求記下來並回 400 ——
  可確認 wiring 與 payload 但不動正式資料。
- **dev server 會不定時整頁重載**（HMR），用瀏覽器工具驗證時把「切頁籤 → 等載入 → 操作 → 讀結果」
  寫成**同一個 async IIFE 一次跑完**，分多次呼叫會中途被重載打斷。
- 部署驗證：比對 `dist/assets/index-*.js` 檔名與線上 HTML 引用的檔名，一致才算真的上線
  （查詢時加隨機參數避開快取）。GitHub Actions 約 40~100 秒。
- **Supabase 公開 URL 有 CDN 快取**：Storage 檔案刪掉後舊網址還會回 200 一段時間，
  要帶隨機 query 重查才看得到真實狀態。
- 加欄位的 migration 放 `sql/`，**由使用者在 Supabase SQL Editor 自己執行**。
  注意順序：`loadGatewayOrders` 的 select 清單一旦帶上新欄位，欄位不存在時 Supabase 回 400，
  **整個金流對帳頁會抓不到訂單** —— 所以一定是「先跑 SQL，再部署」。
- 一次性修資料腳本放 `scripts/`（已 gitignore），由使用者自己執行。

---

## 6. 使用者的操作待辦

1. **四筆入帳差異還沒填原因**（見第 1 節表格）。點明細表的差異數字即可填寫，
   蝦皮那筆 −8 依畫面說明應是「扣除前月代開發票總額」，可順便把對應發票號碼與附件一起補上。
2. **3 筆「平台已結算」尚未確認入帳**（兆豐 2 筆、LINE商城 1 筆）。
3. LINE商城那 2 筆缺 `fee_total`／`payable` 的二月訂單要不要補。

> 已完成（前一版待辦）：蝦皮 6~7 月代開發票已開立 `DK13654450`，
> 金額 14,320 涵蓋 5 筆訂單，與應開金額相符；六月 `DK13654250`（8,360）亦為 ✓ 相符。
